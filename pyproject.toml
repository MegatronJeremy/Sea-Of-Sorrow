[project]
name = "sea-of-sorrow"
version = "0.1.0"
description = "PSZ projekat — web scraping i ML na beloj tehnici (tehnomanija.rs)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["kod"]
testpaths = ["tests"]
addopts = "-v --tb=short"
